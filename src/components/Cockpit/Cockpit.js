import React from 'react';

const cockpit = () => {
    return (
        <div>
            <h1>Hi</h1>
            <button
                onClick={this.togglePersonsHandler}>Toggle persons
            </button>
        </div>
    );
};

export default cockpit;